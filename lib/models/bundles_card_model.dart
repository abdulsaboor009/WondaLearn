import 'dart:ui';

class BundlesCardModel {
  final String image, title, subTitle;
  final Color color;
  final bool isLocked;
  BundlesCardModel({
    required this.image,
    required this.title,
    required this.subTitle,
    required this.color,
    required this.isLocked,
  });
}
